<section id="main">
  <input {{action "toggleAll"}} id="toggle-all" type="checkbox">
  <ul id="todo-list">
    {{#each todo in todos}}
      {{todo-item todo=todo tagName="li"}}
    {{/each}}
  </ul>
</section>